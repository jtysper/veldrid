namespace Veldrid
{
    /// <summary>
    /// The type of query to be performed.
    /// </summary>
    public enum QueryType : byte
    {
        /// <summary>
        /// A timestamp query.
        /// </summary>
        Timestamp,
    }
}
